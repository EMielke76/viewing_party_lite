<h1>Welcome to BE Gulag. Enter credentials, comrade</h1>

<%= form_with url: "/login", method: :post, local: true do |f| %>
  <p><%= f.label "Email" %>
    <%= f.text_field :email %></p>
  <p><%= f.label "Password" %>
    <%= f.password_field :password %></p>
  <%= f.submit "Login" %>
<%end%>
